#ifndef _XN_MATH_HPP_
#define _XN_MATH_HPP_

#include <XnTypes.h>

#define PI		3.1415926535897932384626433832795


namespace xn
{
  namespace		math
  {
    XnVector3D		PointsVector(XnPoint3D a, XnPoint3D b);
    float		PointsDistance(XnPoint3D a, XnPoint3D b);
    float		VectorsDotProduct(XnVector3D a, XnVector3D b);
    XnVector3D		VectorsCrossProduct(XnVector3D a, XnVector3D b);
    float		VectorMagnitude(XnVector3D v);
    float		VectorsAngle(XnVector3D a, XnVector3D b);
  }
}

#endif //_XN_MATH_HPP_
